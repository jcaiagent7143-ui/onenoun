"""onenoun — one noun activates a whole method.

Prune the prose in agent skills (SKILL.md, CLAUDE.md, rules files) that only
re-explains a methodology the model was trained on, replace it with the one
term that activates it, and prove by ablation that the skill did not get worse.
"""

__version__ = "0.1.0"
